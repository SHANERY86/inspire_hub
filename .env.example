# Local dev: use DEBUG=True so CSRF allows Vite (localhost:5173) automatically and cookie paths stay site-wide.
# Production: DEBUG=False and set CSRF_TRUSTED_ORIGINS to your real public origins (https://…).
DEBUG=True
SECRET_KEY=change-me-in-production

DB_NAME=inspire_hub
DB_USER=sryan
DB_PASSWORD=sAmsung86
DB_HOST=db
DB_PORT=5432

OCR_SPACE_API_KEY=

# Comma-separated values
ALLOWED_HOSTS=localhost,127.0.0.1,192.168.1.68,aiteigineile.ie
# When DEBUG=True, http://127.0.0.1:5173 and http://localhost:5173 are always merged in.
# Add LAN or production URLs here; required for production when DEBUG=False.
CSRF_TRUSTED_ORIGINS=http://192.168.1.68,https://aiteigineile.ie
